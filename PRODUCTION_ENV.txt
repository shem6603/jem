# Production Environment Variables for GoDaddy
# Copy these values to cPanel "Setup Python App" > Environment Variables
# DO NOT commit this file with actual passwords - this is a template

# Django Security
SECRET_KEY=_-t1$d+1k8(=ginq(te!k049$m20r(m5f2@*(k5ykt3dj*ihe0
DEBUG=False

# Allowed Hosts
ALLOWED_HOSTS=jem.rixsoft.org,www.jem.rixsoft.org

# Database Configuration
DB_NAME=jem_customer
DB_USER=jem_auto
DB_PASSWORD=YOUR_DATABASE_PASSWORD_HERE
DB_HOST=localhost
DB_PORT=3306

# Instructions:
# 1. Replace 'YOUR_DATABASE_PASSWORD_HERE' with your actual database password from GoDaddy cPanel
# 2. Copy each line to cPanel Environment Variables section
# 3. Domain is already set to: jem.rixsoft.org
# 4. Delete this file after deployment (or keep as reference, but don't commit with real passwords)
