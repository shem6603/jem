Django>=4.2,<5.0
python-dotenv>=1.0.0
gunicorn>=20.1.0
whitenoise>=6.5.0
Pillow>=10.0.0
pulp>=2.7.0
django-pwa>=1.0.9
pywebpush>=1.14.0

# Database drivers (install based on your database choice)
# For MySQL/MariaDB (GoDaddy uses MySQL/MariaDB):
# Try mysqlclient first, if it fails use pymysql
# mysqlclient>=2.1.0  # Uncomment if you can install it
pymysql>=1.1.0  # Alternative MySQL driver (works on Windows)
# For PostgreSQL (alternative):
# psycopg2-binary>=2.9.0
# SQLite is included with Python (for local development only)
